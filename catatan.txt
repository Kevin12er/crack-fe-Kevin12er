next step 

Phase 2.2 :

1. Build the Homepage structure including the navigation bar and footer.✅ (navbar.jsx dan footer.jsx)
2. Populate the Homepage with placeholder content and sections.✅ (Hero section)
3. Build and populate the other page structure.✅ (Dashboard siswa dan guru sudah selesai yang tersisa hanya bagian untuk halaman materi)

Phase 3.3 

1. Initialize the backend project environment and define the folder architecture.
2. Build the database schemas for the core features identified in Phase 1.
3. Create basic CRUD (Create, Read, Update, Delete) API endpoints for the main resource.
4. Seed the database with initial mock data to prepare for frontend integration.


karena waktu tersisa dua minggu, saya harus melakukan penyempitan scope untuk sementara:

yang wajib ada : 

1. Login siswa dan guru 
2. Guru bisa tambah soal
3. siswa bisa kerjakan soal 
4. Guru bisa lihat siapa yang sudah mengerjakan

yang tertunda : 
1. Bridge gate otomatis
2. Node logic berantai 
3. progress tracking detail
4. Dashboard analytic



Note bagus:
1. biasanya kan di button itu ada tanda panah kecil untuk ui navigasi, bisa tambahkan &rarr; untuk panah kecil yang mengarah kekanan dan &larr untuk panah kecil yang mengarah kekiri/ kembali.



Yang masih kurang paling penting:

-Backend dan database
-Real auth + role-based access control
-API login/register
-CRUD soal
-Simpan jawaban siswa
-Skor dan status pengerjaan
-Protected routes sesuai role guru/siswa
-Data siswa kelas/assignment
-Timer ujian, history pengerjaan, review soal
-Validasi, error handling, security


flow bisnis: 

Guru:
Login -> buat soal/bank soal -> assign soal ke kelas -> lihat hasil ujian -> lihat nilai dan rerata 

siswa: 
Login -> lihat materi -> kerjakan soal -> submit jawaban -> lihat hasil -> progress

Backend API yang harus siap
Contoh endpoint:
POST /api/auth/register
POST /api/auth/login
GET /api/me
GET /api/guru/soal
POST /api/guru/soal
GET /api/siswa/soal
POST /api/siswa/submit
GET /api/guru/hasil
GET /api/siswa/histori
Validasi dan keamanan
validasi input
CORS
rate limit
CSRF kalau pakai cookie
authorization per role
audit log
error handling konsisten
pagination & filtering data
Database
Kalau mau serius, pilih:
PostgreSQL kalau data dan query kompleks
MySQL juga oke
MongoDB hanya kalau struktur data sangat dinamis
Deployment-ready
env config
logging
structured API response
error codes konsisten
CI/CD
test basic: auth, CRUD soal, submit ujian, hasil nilai
Frontend harus diubah dari mock menjadi konsumsi API
Saat ini banyak tombol/submit masih cuma console.log dan router push. Itu artinya frontend belum terhubung ke backend. Untuk production:
login fetch ke API
register simpan ke DB
soal diambil dari API
hasil siswa disimpan ke backend
data dashboard dinamis



 4 Perintah Keren yang Wajib Anda Coba SekarangSilakan buka salah satu file kode Anda, blok beberapa baris kode, lalu ketik perintah berikut di panel chat:
 /explain — Meminta Copilot menjelaskan cara kerja kode tersebut dengan bahasa yang mudah dipahami.
 /fix — Menyuruh Copilot mencari tahu jika ada bagian yang bug, error, atau bisa bikin program Anda macet.
 /tests — Membuatkan unit test (kode penguji) otomatis untuk fungsi yang baru saja Anda buat.
 /help — Menampilkan bantuan dan tutorial langsung dari tim GitHub tentang apa saja yang bisa dilakukan oleh Copilot Chat.